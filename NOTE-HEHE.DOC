// tạo module 

nest g resource [tên module] --no-spec
nest g resource jobs --no-spec
nest g resource jobTypes --no-spec
nest g resource hireJob --no-spec
nest g resource skill --no-spec
nest g resource jobs-detail --no-spec


// kết nối server sử dụng Prisma
 - yarn add prisma @prisma/client
 - yarn prisma init
 - vào .env sửa lại chuỗi kết nối dữ liệu
 - yarn prisma db pull
 - yarn prisma ganerate



 // COI LẠI PHẦN XÓA DÍNH ĐẾN QUAN HỆ GIỮA CÁC BẢNG
 // OK

 // xử lý lại updateComment
 // createJobDeatail
 // upload hình ảnh (job)
 // menu (job)
 

 catch (exception) {
        if (exception.status != 500)
          throw new HttpException(exception.response, exception.status);
        throw new HttpException("Lỗi ....", HttpStatus.INTERNAL_SERVER_ERROR);
      }
 